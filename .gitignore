# ── Dependencies ─────────────────────────────────────────────────────────────
node_modules/
.pnp
.pnp.*

# ── Build outputs ─────────────────────────────────────────────────────────────
.next/
out/
apps/web/.next/
apps/web/out/
apps/web/build/
packages/*/dist/
dist/

# ── Expo / Mobile ─────────────────────────────────────────────────────────────
apps/mobile/.expo/
apps/mobile/android/
apps/mobile/ios/

# ── Turbo cache ───────────────────────────────────────────────────────────────
.turbo/
packages/*/.turbo/

# ── Runtime data server (dữ liệu live, không commit) ─────────────────────────
# apps/web/src/data/ (seed files tĩnh) vẫn được commit
apps/web/data/history.json
apps/web/data/users.json
apps/web/data/materials.json
apps/web/data/constants.json
apps/web/data/profit-table.json
apps/web/data/production-orders.json

# ── TypeScript ────────────────────────────────────────────────────────────────
*.tsbuildinfo
next-env.d.ts

# ── Env ──────────────────────────────────────────────────────────────────────
.env
.env.local
.env*.local
.env.web_local

# ── Flutter dart-define (per-developer; copy từ .dart_defines.example.json) ─
.dart_defines.json
apps/flutter_app/.dart_defines.json

# ── Logs & debug ─────────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── File phân tích / ghi chú tạm (không phải source code) ────────────────────
CODEBASE_ANALYSIS_FINAL.txt
CUSTOMER_ANALYSIS_FINAL.txt
CYLINDER_QUICK_REFERENCE.txt
CYLINDER_TRUC_IN_ANALYSIS.txt
LAYOUT_FINAL_REPORT.txt
RESPONSIVE_DESIGN_ANALYSIS.txt
RESPONSIVE_SUMMARY.txt
analysis.txt
findings.txt
LSX_EXPORT_BUG_REPORT.txt

# ── package-lock.json (dùng pnpm rồi, không cần file này nữa) ────────────────
package-lock.json

# ── Misc ─────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.pem
.vercel
coverage/

# ── LTS API helper (PowerShell) ───────────────────────────────────────────────
# Script chính: scripts/lts-api.ps1 (commit). Backup/temp không commit.
scripts/*.bak
scripts/lts-api.ps1.bak

# Credentials local (KHONG BAO GIO commit)
scripts/.lts-credentials.local.ps1
*.local.ps1
